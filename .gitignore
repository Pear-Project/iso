# Chroot build directories (rootfs-base, rootfs-target, ISO staging)
build/

# Generated ISO output (now written to the repo root, not build/)
*.iso

# Log and debug files
*.log
*.tmp

# QEMU temporary files
.qemu_9p/
__pycache__/
